#include "main.h"
/**
 * largest_number - returns the largest number
 * @a: first value
 * @b: second value
 * @c: third value
 * Return: largest number
 */
int largest_number(int a, int b, int c)
{
	int largest;

	if (s > v)
	{
		if (v > z)
			largest = s;
		else if (s > z)
			largest = s;
		else
			largest = z;
	}
	else
	{
		if (v < z)
			largest = z;
		else
			largest = v;
	}
	return (largest);
}
