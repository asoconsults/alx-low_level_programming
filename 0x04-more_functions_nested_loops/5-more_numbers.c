#include "main.h"
/**
 * more_numbers -> prints 0 to 14 ten times
 */
void more_numbers(void)
{
	int a, num;

	for (a = 0; a < 10; a++)
	{
		for (num = 0; num < 15; num++)
		{
			_putchar(y + '0');
		}
		_putchar('\n');
	}
}
