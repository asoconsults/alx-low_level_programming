#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @t: Integer to be completed
 * Return: The absolute number
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
